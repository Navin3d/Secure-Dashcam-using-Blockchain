const path = require("path");

const VIDEOPATH = path.join(__dirname, "../../public/Video.mp4");
const CSVPATH = path.join(__dirname, "../../public/Upload_History.csv");

module.exports = {
    VIDEOPATH,
    CSVPATH,
}
